# 秒杀活动管理功能设计方案

## 需求

为管理员提供秒杀活动的基础 CRUD：
- 创建活动（支持初始绑定商品）
- 添加商品到已有活动
- 修改活动状态（手动暂停/恢复）
- 自动状态切换（根据时间）

## 后端 API 地址（已完成）

| 方法 | 路径 | 说明 |
|------|------|------|
| POST | `/admin/flash-sale/create` | 创建活动（可选初始商品） |
| GET | `/admin/flash-sale/{id}` | 获取活动详情 |
| POST | `/admin/flash-sale/{id}/products` | 添加商品到活动 |
| PUT | `/admin/flash-sale/{id}/status` | 修改活动状态 |
| DELETE | `/admin/flash-sale/{id}` | 删除活动 |

## 前端 API 地址（待实现）

前端 baseURL 为 `/api`，代理到 `http://localhost:8082`，所以前端实际调用地址：

| 方法 | 前端地址 | 后端地址 | 说明 |
|------|----------|----------|------|
| POST | `/api/admin/flash-sale/create` | `/admin/flash-sale/create` | 创建活动 |
| GET | `/api/admin/flash-sale/{id}` | `/admin/flash-sale/{id}` | 获取活动详情 |
| POST | `/api/admin/flash-sale/{id}/products` | `/admin/flash-sale/{id}/products` | 添加商品 |
| PUT | `/api/admin/flash-sale/{id}/status` | `/admin/flash-sale/{id}/status` | 修改状态 |
| DELETE | `/api/admin/flash-sale/{id}` | `/admin/flash-sale/{id}` | 删除活动 |

## 前端文件清单

**新建：**
1. `frontend/src/api/flash-sale.ts` — 秒杀活动 API 接口定义

**修改：**
1. `frontend/src/views/admin/` — 添加秒杀活动管理页面（如需要）

## 前端 API 接口定义

```typescript
// frontend/src/api/flash-sale.ts

import request, { type Result } from '../utils/request'

export interface FlashSaleCreateRequest {
  title: string
  startTime: string
  endTime: string
  products?: FlashSaleProductItem[]
}

export interface FlashSaleProductItem {
  productId: number
  flashPrice: number
  totalStock: number
  maxPerUser: number
}

export interface FlashSaleAddProductRequest {
  productId: number
  flashPrice: number
  totalStock: number
  maxPerUser: number
}

export interface FlashSaleStatusRequest {
  status: number // 0未开始 1进行中 2已结束
}

export interface FlashSaleVO {
  id: number
  title: string
  startTime: string
  endTime: string
  status: number
  products?: FlashSaleProductVO[]
}

export interface FlashSaleProductVO {
  id: number
  productId: number
  productName: string
  productImage: string
  originalPrice: number
  flashPrice: number
  totalStock: number
  remainingStock: number
  maxPerUser: number
}

// 创建秒杀活动
export const createFlashSaleApi = (data: FlashSaleCreateRequest) => {
  return request.post<Result<number>>('/admin/flash-sale/create', data)
}

// 获取秒杀活动详情
export const getFlashSaleDetailApi = (id: number) => {
  return request.get<Result<FlashSaleVO>>(`/admin/flash-sale/${id}`)
}

// 添加商品到秒杀活动
export const addFlashSaleProductApi = (id: number, data: FlashSaleAddProductRequest) => {
  return request.post<Result<null>>(`/admin/flash-sale/${id}/products`, data)
}

// 修改秒杀活动状态
export const updateFlashSaleStatusApi = (id: number, data: FlashSaleStatusRequest) => {
  return request.put<Result<null>>(`/admin/flash-sale/${id}/status`, data)
}

// 删除秒杀活动
export const deleteFlashSaleApi = (id: number) => {
  return request.delete<Result<null>>(`/admin/flash-sale/${id}`)
}
```

## 验证

```bash
# 运行后端测试
./mvnw test

# 前端启动后验证
cd frontend && npm run dev
```
